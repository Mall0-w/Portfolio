import { Box } from "@mui/material"
import { forwardRef } from "react"

const Contact = forwardRef((props) => {
    return (
        <Box id="contact" height="30vh" width="100%">
        </Box>
    )
})

export default Contact